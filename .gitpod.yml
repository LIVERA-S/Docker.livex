# List the start up tasks. Learn more https://www.gitpod.io/docs/config-start-tasks/
tasks:
  - name : AngularWebServer
    init: cd schoolFrontEnd && npm i -g @angular/cli@13.2.0 && npm i # runs during prebuild
    
  - name: Go back and start Docker Compose
    init: docker compose up
